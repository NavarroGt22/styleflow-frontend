# URL da API (backend) — obrigatório em produção
VITE_API_URL=https://api.seudominio.com

# WebSocket da API — opcional (derivado de VITE_API_URL se omitido)
# VITE_WS_URL=wss://api.seudominio.com

# Domínio base da plataforma (subdomínios = tenants)
VITE_BASE_DOMAIN=seudominio.com
